# Binaries — built example programs end up at repo root when run as `go build .`
*.exe
*.exe~
*.dll
*.so
*.dylib
/mcp-server
/example-bin

# Test binary
*.test

# Output
*.out

# Coverage
*.cov
coverage.out
coverage.html
.cover/*
!.cover/history.json

# Security scanning — track suppression baseline, ignore scan outputs
.nox/*
!.nox/baseline.json
findings.json

# IDE
.idea/
.vscode/
*.swp
*.swo

# Claude Code local state
.claude/

# Relicta
.relicta/

# Roady — version-control spec/plan/policy/state, ignore append-only
# event log and ephemeral telemetry.
.roady/events.jsonl
.roady/usage.json

# OS
.DS_Store
